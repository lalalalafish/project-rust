fn main() {
    //1.1 line comments
    println!("Hello, world!");//1.2 line comments
    //1.3 line comments are the recommended comment style 

    /* 2.1 but the block comments are extremely useful for temporarily disabling chunks of code. */
    /* /* 2.2 Block comments can be nested. So it takes only a few keystrokes to comment out eerything in this main() function */ */
    /* 2.3
     * The previous column of `*` was entirly for style. 
     * There's no actual need for it.
     */
    /* 2.4
    block comment
    block comment
     */
}
